# 提交前检查清单

## 已准备

- [x] 使用真实 OpenAI-compatible LLM API。
- [x] 自研 Agent Loop 与结构化工具调用闭环。
- [x] 根目录 README 包含运行方式和系统架构。
- [x] README 说明 Memory 的触发时机、存储位置和召回顺序。
- [x] AI Prompt 与问题解决记录已经整理。
- [x] 离线测试、静态检查和真实 LLM 验收证据已经记录。
- [x] `.env`、数据库、虚拟环境、IDE 文件和录屏文件已加入忽略规则。
- [x] 操作录屏已由候选人完成。

## 上传前需要候选人完成

- [ ] 在 GitHub 创建空仓库。
- [ ] 配置 `origin` 并推送 `main`。
- [ ] 将 GitHub 浏览器页面 URL 填入提交页面。
- [ ] 上传录屏并填写可访问 URL。
- [ ] 使用无痕窗口验证仓库与录屏链接权限。

## 安全复查

```powershell
git status
git check-ignore -v .env
git ls-files
```

确认 `git ls-files` 中不存在：

```text
.env
.venv/
venv/
data/agent.db
.idea/
任何录屏文件
```

不得在提交页面、README、Issue、Commit message 或录屏中展示 API Key、Authorization
Header、Personal Access Token 和私人账号信息。
