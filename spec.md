# 最小可用 Agent Runtime 规格说明

## 背景

本项目用于完成“2026 年 Agent 技术笔试题”的 Vibe Coding 部分：从零实现一个最小可用 Agent。题目允许使用 AI 工具辅助开发，但禁止依赖 LangGraph、OpenHands、OpenClaw 等现有 Agent 框架完成主流程，核心 Agent Runtime 必须自行实现。

本阶段只覆盖 Vibe Coding 题目，不包含后续五选一的架构设计题。最终产品以可运行、可测试、可录屏演示为目标，同时通过简洁网页界面和终端入口展示核心 Runtime 能力。

## 目标

- 自行完成“接收用户输入—请求真实 LLM—直接回复或调用工具—回填结果—继续循环”的 Agent 基本闭环。
- 用至少三个具有明确 Schema 的工具证明模型能够自主选择工具并正确传参。
- 支持相互隔离、可持续对话的多个 Session，并在追问时保留必要状态。
- 在上下文超过预算时进行基础压缩，使长对话仍可继续。
- 让 Agent 的关键运行事件、工具调用和异常可观察、可排查。
- 提供网页和终端两种演示入口，以及完整的测试、说明和 AI 辅助开发记录。

## 功能需求

### F1：自研 Agent 基本循环

系统必须自行实现 Agent 主循环。每轮应接收用户输入，调用真实 LLM，并根据模型输出决定直接回答还是调用工具。工具执行后，结果应作为新信息返回给模型；系统应继续循环，直至产生最终回答、发生不可恢复错误或达到最大轮次。

### F2：真实 LLM 调用与输出解析

系统必须接入真实可用的 LLM API，不得仅使用模拟模型完成最终演示。系统应能区分并处理模型产生的最终回答与结构化工具调用。运行日志只记录可观察的决策事件和结果摘要，不要求、也不得伪造或暴露模型隐藏的完整思维链。

### F3：工具注册与发现

系统必须提供统一的工具注册机制。每个工具至少包含唯一名称、用途描述和参数 Schema。当前可用工具信息应随模型请求提供，使模型能够基于名称、描述和 Schema 自主选择工具及构造参数。

### F4：至少三个可演示工具

系统必须至少提供以下三类工具：

- 计算器：完成受控的基础算术计算。
- 模拟搜索：根据查询返回稳定、可复现的模拟搜索结果。
- 待办事项：支持创建和查看当前 Session 的待办事项。

三个工具均应通过统一注册与执行流程调用，不允许为某个演示问题硬编码固定调用路径。

### F5：工具调用校验与结果回填

工具执行前必须验证工具是否存在、参数是否满足 Schema。工具执行结果或可处理的失败信息必须回填到当前对话上下文，以便模型决定继续调用工具还是向用户返回结果。

### F6：多 Session 管理

系统必须允许创建和切换多个独立 Session。同一用户在不同窗口或入口中发起的任务不得共享对话历史和 Session 级待办数据。返回已有 Session 后，应能基于该 Session 的历史继续对话。

### F7：连续对话与追问

系统必须支持纯对话追问和带工具追问。追问时应保留回答所需的既有信息，例如用户约束、相关历史消息、工具调用及工具结果，同时避免把其他 Session 的内容带入当前请求。

### F8：上下文预算与最大轮次

系统必须允许配置单次 Agent 运行的最大循环轮次和上下文预算。达到最大轮次时应停止继续调用，并向用户返回明确、可理解的终止信息，而不是无限循环或静默失败。

### F9：基础上下文压缩与记忆召回

当历史内容接近或超过上下文预算时，系统必须进行基础压缩。压缩后应保留当前任务所需的近期消息、重要用户事实、必要的工具结果和历史摘要。摘要记忆应与对应 Session 绑定，并在该 Session 后续请求构造上下文时被召回。

### F10：异常处理

系统必须对常见失败提供可理解的处理，包括 LLM 请求失败、输出无法解析、未知工具、参数无效、工具执行异常和最大轮次终止。一次请求失败不应破坏其他 Session，也不应导致服务整体退出。

### F11：Trace 与执行日志

每次 Agent 运行必须形成可关联的 Trace，至少记录 Session 标识、运行标识、轮次、模型调用状态、工具名称、经校验的工具参数、工具执行状态、耗时和错误类型。敏感凭据、完整隐藏思维链及不必要的私人内容不得写入日志。

### F12：网页与终端交互

系统必须提供简洁网页聊天界面作为主要演示入口，并提供终端入口用于快速运行和调试。两种入口必须调用同一套 Agent Runtime，并支持指定或切换 Session。网页应能够区分用户消息、Agent 最终回答和工具执行事件。

### F13：测试用例

系统必须提供自动化测试，覆盖直接回答、单工具调用、多轮工具调用、三个工具的注册与执行、参数校验、Session 隔离、连续追问、上下文压缩、最大轮次和主要异常路径。真实 LLM 端到端验证应与可重复执行的自动化测试区分，避免日常测试完全依赖外部网络。

### F14：提交材料

最终提交必须包含：

- 可访问的代码仓库链接。
- 终端或网页完整操作录屏。
- README，包括运行方式、系统设计、Agent Loop、工具机制、Session/Context 设计、Memory 的召回时机与放置方式，以及测试方法。
- AI Prompt 与问题解决记录，说明 AI 在需求分析、设计、实现、调试和验证中的具体作用，并记录重要问题及解决过程。

## 非功能需求

- N1：核心 Runtime 必须自行实现，不得用现有 Agent 框架代理主循环、工具调度、Session 或 Context 管理。
- N2：主实现采用 Python，代码结构应便于面试时讲解模块职责和运行链路。
- N3：所有密钥和外部服务配置必须通过运行环境提供，不得提交到代码仓库、日志或录屏画面中。
- N4：在正常本地开发环境中，安装、启动和首次演示应有明确步骤，避免依赖人工修改源码。
- N5：工具参数、Session 数据和模型输出必须在进入关键执行路径前进行边界校验。
- N6：核心功能应可在没有网页界面的情况下独立测试，网页不得承载 Runtime 业务逻辑。
- N7：项目范围以清晰、正确、可解释的最小实现为主，避免为展示数量堆叠与题目无关的功能。

## 不在范围内

- 五选一架构设计题及其文档。
- 多 Agent 协作、复杂任务规划器、自主浏览器或计算机控制。
- 向量数据库、知识库 RAG、复杂语义记忆和复杂压缩算法。
- 生产级用户认证、权限系统、计费、租户隔离和云端高可用部署。
- 真实互联网搜索服务；题目允许搜索工具使用稳定 Mock。
- 复杂前端设计、移动端适配及与核心 Agent 演示无关的页面。
- 收集、展示或持久化模型不可见的隐藏思维链。

## 验收标准

| 编号 | 对应需求 | 可观察的验收结果 |
| --- | --- | --- |
| AC1 | F1, F2 | 使用真实 LLM 提交普通问题时，Agent 不调用工具即可返回有效答案；提交工具型问题时，Agent 能完成至少一次“模型决策—工具执行—结果回填—最终回答”闭环。 |
| AC2 | F3, F4 | 系统可列出至少三个包含名称、描述和参数 Schema 的工具；计算器、模拟搜索、待办事项均能由模型自主选择并成功执行。 |
| AC3 | F5, F10 | 请求未知工具或传入无效参数时，系统拒绝危险执行，产生明确错误事件，并允许 Agent 或用户继续交互。 |
| AC4 | F6 | 创建两个 Session，分别执行搜索类对话和待办对话后，任一 Session 都看不到另一 Session 的历史与待办数据；重新进入原 Session 可继续原话题。 |
| AC5 | F7 | Agent 能正确回答依赖前文的纯对话追问，并能在后续追问中利用先前工具结果或再次选择工具。 |
| AC6 | F8 | 将最大轮次设置为较小值并触发未完成循环时，运行在限制内停止，并返回明确的最大轮次终止信息。 |
| AC7 | F9 | 构造超过上下文预算的长对话后，系统产生压缩摘要；后续请求仍能使用保留的重要事实完成追问，且摘要不会跨 Session 出现。 |
| AC8 | F10 | 模拟 LLM 超时、无法解析的模型输出和工具异常时，系统返回受控错误，服务仍能处理下一次请求和其他 Session。 |
| AC9 | F11 | 一次含工具调用的运行可查询或观察到完整事件链，包括运行标识、轮次、模型与工具状态、耗时及错误信息，同时日志中不出现 API Key。 |
| AC10 | F12 | 网页和终端均可完成同一类直接回答及工具调用任务，并能在指定 Session 中继续对话；网页可直观看到工具事件与最终回答。 |
| AC11 | F13 | 自动化测试覆盖规格列出的核心正常与异常路径，并可通过单一测试命令重复运行；另有明确的真实 LLM 端到端验证步骤。 |
| AC12 | F14 | 仓库包含完整 README 和 AI Prompt/问题解决记录；录屏能够展示启动、多 Session、三类工具、追问、Trace 及异常处理的代表性流程。 |
